#include "main.h"

/**
 * _abs - convert the negative number to positive
 * @n: the number
 *
 * Return:
 *     The absolute number of n
 */

int _abs(int n)
{
if (n < 0)
{
return (n * -1);
}
return (n);
}
